// MRoomView is a join table (roomId + masterRoomViewId) — no human-readable string fields.
// Translations are handled via MasterRoomViewTranslation in property-masters.model.ts
// This file is intentionally a no-op placeholder.

export {};
